# Python
backend/venv/
backend/__pycache__/
*.pyc

# Local data — never commit real user data or recordings
backend/app.db
backend/users.db
backend/recordings/

# Evidence-layer signing key — see backend/evidence.py. Keep this
# private per-deployment; don't commit or share it, and don't copy it
# between environments (a receipt only verifies against the exact
# public key that sealed it).
backend/evidence_keys/

# Secrets
backend/.env
.env

# OS/editor noise
.DS_Store
*.log
